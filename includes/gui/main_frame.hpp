#ifndef MAIN_FRAME_HPP
#define MAIN_FRAME_HPP

#include <wx/wx.h>

/**
 * The main frame of the GUI.
 */
class MainFrame: public wxFrame {
public:

    // Constructors
    MainFrame();

    // Destructor
    ~MainFrame();

    // Getters

    // Setters

    // Instance's methods

private:

};

#endif